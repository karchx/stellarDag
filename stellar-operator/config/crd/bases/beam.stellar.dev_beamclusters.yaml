---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: beamclusters.beam.stellar.dev
spec:
  group: beam.stellar.dev
  names:
    kind: BeamCluster
    listKind: BeamClusterList
    plural: beamclusters
    singular: beamcluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BeamCluster is the Schema for the beamclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BeamCluster
            properties:
              cookieSecret:
                type: string
              image:
                type: string
              replicas:
                description: Replicas define total nodes
                format: int32
                minimum: 1
                type: integer
            required:
            - cookieSecret
            - image
            - replicas
            type: object
          status:
            description: status defines the observed state of BeamCluster
            properties:
              readyReplicas:
                format: int32
                type: integer
            required:
            - readyReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
