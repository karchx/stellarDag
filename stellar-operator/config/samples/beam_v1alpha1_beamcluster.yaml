apiVersion: beam.stellar.dev/v1alpha1
kind: BeamCluster
metadata:
  labels:
    app.kubernetes.io/name: stellar-operator
    app.kubernetes.io/managed-by: kustomize
  name: beamcluster-sample
spec:
  # TODO(user): Add fields here
