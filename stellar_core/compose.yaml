volumes:
  db_local:

services:
  postgres:
    image: postgres:latest
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: stellar_core
    ports:
      - "5432:5432"
    volumes:
      - db_local:/var/lib/postgresql
